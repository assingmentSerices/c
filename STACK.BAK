#define MAX_SIZE 10
int stack[MAX_SIZE];
int top=-1;

void peek()
{
printf("peek=>%d"stack[top]);
}

void pop()
{
printf("pop=>%d",stack[top--]);
//return ;
}
int isempty()
{
return top==-1?1:0;
}

int push(int data)
{
printf("push=>%d",data);
if(!isfull())
return stack[++top]=data;
}

int isfull()
{
return top==MAX_SIZE-1?1:0;
}

void main()
{
clrscr();
push(32);
push(33);
push(45);
pop();
peek();
getch();
}





















































































































































































































































































































































































































































































































































































































































