#include<stdio.h>
#include<conio.h>
int getlastind(char str[],int i);
{
 while(1<=strlen(str))
 {
  if(str[i]==" "||str[i]==0)
  return i-1;
  i++;
  }
  }
  void restr(char str[],int ind)
  { int s=ind;
  int e;
  char c;
  if(end==1)
  {
   s=0;
   e=strlen(str)-1;
   }
   else
   {
   e=getlastind(strind);
   }
   while(s<e)
   {
    c=str[s];
    str[s]=str[e];
    str[e]=c;
    s++;
    e--;
    }
    }
    voiod main()
    {
    char s[]="hello this is dollop";
    int i;
    clrscr();
    printf("%s\n",s);
    revstr(s,-1);
    for(i=0;s[i]=" ";i++)
    {
    if(i==0||(s[i-1]==" "&&s[i]!=" "))
    revstr(s,i);
    }
    printf("%s ",s);
    getch();
}



















































































































































































































































































































































































































































































































































































































































